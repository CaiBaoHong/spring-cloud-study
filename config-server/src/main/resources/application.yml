server:
  port: 7001
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/CaiBaoHong/spring-cloud-config-repo.git
          search-paths: spring_cloud_study
eureka:
  client:
    service-url:
      defaultZone: http://localhost:1111/eureka